# Build tooling (helps pip choose wheels and avoid building from source)
pip>=25
setuptools>=75
wheel>=0.44

# Prefer prebuilt wheels (avoid metadata-generation-failed from sdists)
--only-binary=:all:

# Core
streamlit>=1.34
pandas>=2.2
numpy>=1.26

# Visuals / PDF / optimisation (versions with manylinux wheels for Py3.11/3.12)
matplotlib==3.8.4
PyMuPDF==1.24.10
scipy==1.12.0
plotly==5.22.0
openpyxl==3.1.2

# Optional (for HAC / Newey–West in Factor Regression)
statsmodels==0.14.1
